extends layout

block content
  h1= title
  p this page is #{title}

  form(action="/up", method="POST")
    input(type="file",name="avatar")
    input.btn(type="submit", name="submit", value="submit")
